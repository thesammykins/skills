# npx-managed skills

These skills are present in the synchronized snapshot because they currently exist under `~/.agents/skills`, but their installation is managed by `npx skills@1.5.16`. Refresh them through that installer rather than treating this repository as their upstream source.

- `addressing-github-issues` — install or refresh with `npx skills@1.5.16`.
- `agent-browser` — install or refresh with `npx skills@1.5.16`.
- `agent-change-verification` — install or refresh with `npx skills@1.5.16`.
- `anti-ai-slop` — install or refresh with `npx skills@1.5.16`.
- `bounded-work-session` — install or refresh with `npx skills@1.5.16`.
- `codebase-inspection` — install or refresh with `npx skills@1.5.16`.
- `configuring-dependabot` — install or refresh with `npx skills@1.5.16`.
- `convert-prompt` — install or refresh with `npx skills@1.5.16`.
- `council` — install or refresh with `npx skills@1.5.16`.
- `creating-conventional-branches` — install or refresh with `npx skills@1.5.16`.
- `creating-conventional-commits` — install or refresh with `npx skills@1.5.16`.
- `creating-github-sub-issues` — install or refresh with `npx skills@1.5.16`.
- `cua-driver` — install or refresh with `npx skills@1.5.16`.
- `diagnose-ci-failures` — install or refresh with `npx skills@1.5.16`.
- `dogfood` — install or refresh with `npx skills@1.5.16`.
- `done` — install or refresh with `npx skills@1.5.16`.
- `gemini-large-context` — install or refresh with `npx skills@1.5.16`.
- `gh-cli` — install or refresh with `npx skills@1.5.16`.
- `git-master` — install or refresh with `npx skills@1.5.16`.
- `happy-path-use-case-design` — install or refresh with `npx skills@1.5.16`.
- `hardening-github-actions` — install or refresh with `npx skills@1.5.16`.
- `implement-specs` — install or refresh with `npx skills@1.5.16`.
- `maestro` — install or refresh with `npx skills@1.5.16`.
- `onepassword-agent` — install or refresh with `npx skills@1.5.16`.
- `progressive-production-hardening` — install or refresh with `npx skills@1.5.16`.
- `project-skills` — install or refresh with `npx skills@1.5.16`.
- `pushing-and-creating-pull-request` — install or refresh with `npx skills@1.5.16`.
- `recurring-operational-monitor` — install or refresh with `npx skills@1.5.16`.
- `reproduce-bug-report` — install or refresh with `npx skills@1.5.16`.
- `resolve-merge-conflicts` — install or refresh with `npx skills@1.5.16`.
- `respond-to-pr-comments-in-blocklist` — install or refresh with `npx skills@1.5.16`.
- `review-pr` — install or refresh with `npx skills@1.5.16`.
- `simplify` — install or refresh with `npx skills@1.5.16`.
- `spec-driven-implementation` — install or refresh with `npx skills@1.5.16`.
- `spike` — install or refresh with `npx skills@1.5.16`.
- `systematic-debugging` — install or refresh with `npx skills@1.5.16`.
- `update-skill` — install or refresh with `npx skills@1.5.16`.
- `validate-changes-match-specs` — install or refresh with `npx skills@1.5.16`.
- `validating-agent-skills` — install or refresh with `npx skills@1.5.16`.
- `write-product-spec` — install or refresh with `npx skills@1.5.16`.
- `write-tech-spec` — install or refresh with `npx skills@1.5.16`.
- `writing-plans` — install or refresh with `npx skills@1.5.16`.

`.system` is excluded separately because it is Codex-managed.
