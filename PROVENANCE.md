# Provenance

`happy-path-use-case-design` is vendored as a locally maintained skill copied from the Hermes installation. It is the only skill intentionally checked into this repository.

`.system` is managed by Codex and must not be copied or redistributed. Skills listed in `NPIX_INSTALL_NOTES.md` are installed by `npx skills@1.5.16`; keep them out of this repository and document their installer association instead.
